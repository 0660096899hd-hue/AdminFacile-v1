name: admin_facile
description: Assistant administratif accessible avec modèles et scanner OCR local.
publish_to: "none"
version: 2.2.0+5

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.2.3
  google_mlkit_text_recognition: ^0.16.0
  shared_preferences: ^2.5.5
  file_picker: 10.3.10
  speech_to_text: ^7.4.0
  google_mlkit_translation: ^0.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
